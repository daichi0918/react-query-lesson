import { Layout } from './components/Layout'
import { BrowserRouter, Route, Routes } from 'react-router-dom'

export const App = () => (
  <BrowserRouter>
    <Layout>App</Layout>
  </BrowserRouter>
)
